#include "dim.cuh"

// Content moved to iteration.cu to resolve symbol visibility issues with
// separate compilation. This file is kept to avoid breaking build scripts that
// expect it, or can be removed if CMake allows.
